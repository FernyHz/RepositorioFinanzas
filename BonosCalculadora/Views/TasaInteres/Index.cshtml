@model IEnumerable<BonosCalculadora.Models.TasaInteres>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tasa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipotasa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tasa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipotasa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TasaInteresId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TasaInteresId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TasaInteresId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
